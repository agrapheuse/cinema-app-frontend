/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.destudio.com',
      'www.lumiere-antwerpen.be',
      'cinemacartoons.be',
    ],
  },
}

export default nextConfig
